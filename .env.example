# SwitchBot API Configuration
SWITCHBOT_TOKEN=your_switchbot_token_here
SWITCHBOT_SECRET=your_switchbot_secret_here

# SSH Configuration for Remote Reboot
# Required: Host IP address of the computer to reboot
SSH_HOST=192.168.0.100

# Required: Username for SSH connection
SSH_USERNAME=pi

# Authentication: Use either password OR private key (not both)
# Option 1: Password authentication
SSH_PASSWORD=your_ssh_password_here

# Option 2: Private key authentication (recommended for security)
# SSH_PRIVATE_KEY_PATH=/path/to/your/private/key

# Optional: SSH port (default is 22)
SSH_PORT=22

# Notes:
# 1. For password authentication, make sure the user has sudo privileges without password prompt
#    or configure sudoers file: echo "username ALL=(ALL) NOPASSWD: /sbin/reboot" | sudo tee /etc/sudoers.d/reboot
# 2. For key authentication, generate SSH keys: ssh-keygen -t rsa -b 4096
#    and copy public key to target: ssh-copy-id username@host
# 3. Test SSH connection manually before using: ssh username@host
